apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui
  namespace: guestbook
  labels:
    app: guestbook-ui
spec:
  selector:
    app: guestbook-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081
  type: NodePort
